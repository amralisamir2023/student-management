// Base URL of the real backend (backend/server.js). Everything under Students
// and Auth talks to this for real; the other four modules are still mocked
// (see each service's file for a note on what to change to connect it).
export const API_BASE = 'http://localhost:5000/api';
